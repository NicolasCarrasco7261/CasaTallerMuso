name: desplegar frontend a github pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd Producto/FrontEnd
          npm ci

      - name: Build
        run: |
          cd Producto/FrontEnd
          npm run build
        env:
          VITE_REACT_APP_BACKEND_URL: ${{ vars.API_PROTOCOL }}://${{ vars.REMOTE_HOST }}:${{ vars.API_PORT }}

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: Producto/FrontEnd/dist

## test
